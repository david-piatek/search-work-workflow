{
  "name": "job-scraper-app",
  "version": "1.0.0",
  "description": "Job scraping application with dynamic scraper generation",
  "private": true,
  "workspaces": [
    "apps/frontend",
    "apps/backend"
  ],
  "scripts": {
    "dev": "task dev",
    "build": "task build",
    "test": "task test",
    "lint": "task lint",
    "format": "prettier --write \"**/*.{ts,js,svelte,json,md}\""
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "prettier": "^3.1.0",
    "prettier-plugin-svelte": "^3.1.2",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
