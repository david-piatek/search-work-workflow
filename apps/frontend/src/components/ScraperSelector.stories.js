import ScraperSelector from './ScraperSelector.svelte';

export default {
  title: 'Components/ScraperSelector',
  component: ScraperSelector,
  tags: ['autodocs'],
};

export const Default = {
  args: {},
};
